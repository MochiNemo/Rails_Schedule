<h3>スケジュール詳細</h3>

<table class = "show-table">
  
  <tr>
    <th>タイトル</th>
    <td><%= @schedule.title %></td>
  </tr>
  <tr>
    <th>開始日</th>
    <td><%= @schedule.start_date.to_s(:datetime_ja) %></td>
  </tr>
  <tr>
    <th>終了日</th>
    <td><%= @schedule.end_date.to_s(:datetime_ja) %></td>
  </tr>
  <tr>
    <th>終日</th>
    <td>
      <% if @schedule.all_day %>
        ○
      <% end %>
    </td>
  </tr>
  <tr>
    <th>スケジュールメモ</th>
    <td class = "memo"><pre><%= @schedule.memo %></pre></td>
  </tr>
</table>

<div>
  <ul>
    <li><%= link_to "スケジュール一覧に戻る", :schedules %></li>
    <li><%= link_to "スケジュール一覧を編集する", :edit_schedule %></li>
    <li><%= link_to "スケジュールを消去する", :schedule, method: :delete, data:{confirm:"本陽に削除しますか？"} %></li>
  </ul>
</div>